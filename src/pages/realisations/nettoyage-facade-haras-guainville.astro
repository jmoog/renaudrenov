---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { SITE } from '../../data/site.ts';
import FilAriane from '../../components/FilAriane.astro';
import AvantApresSlider from '../../components/AvantApresSlider.astro';
import LogoGoogle from '../../components/LogoGoogle.astro';
import FlecheRonde from '../../components/FlecheRonde.astro';
import reviews from '../../data/reviews.json';
import CtaDevis from '../../components/CtaDevis.astro';

const ville = 'Guainville';
const cp = '28260';

// ── Balises différenciées (règle anti-triplette du projet) ──
// Title → « démoussage » : laisse le mot-clé « nettoyage de façade » à la page
// prestation (anti-cannibalisation) et casse la triplette avec H1 + meta.
// H1 et meta = formulations dictées par le client, ne pas les retoucher.
const title = "Démoussage de façade dans un haras à Guainville (28)";
const description =
  "Nettoyage des façades d'un centre équestre à Guainville, dans le 28, par Renaud Rénov.";

const businessId = `${SITE.url}/#localbusiness`;
const pageUrl = `${SITE.url}/realisations/nettoyage-facade-haras-guainville/`;

const img = (n: string) => `/images/photos/nettoyage-facade-haras-guainville-${n}.webp`;

// ── Les trois façades traitées, dans l'ordre du chantier ──
const comparaisons = [
  {
    cle: 'entree',
    titre: "La façade d'entrée",
    legende:
      "Plein sud, la façade d'accueil du bâtiment principal : le premier mur que voient les visiteurs et les cavaliers.",
    altAvant: `Façade d'entrée d'un haras à ${ville} grisée par les algues avant nettoyage`,
    altApres: `La même façade d'entrée à ${ville}, enduit ravivé après le nettoyage`,
  },
  {
    cle: 'pignon',
    titre: 'Le pignon nord-ouest',
    legende:
      "Le mur le plus attaqué : traînées rouges de ruissellement, voile vert sur tout le soubassement et lierre en pied de façade.",
    altAvant: `Pignon d'un haras à ${ville} couvert de mousses et de coulures rouges avant intervention`,
    altApres: `Le même pignon à ${ville} débarrassé des mousses et des traînées après nettoyage`,
  },
  {
    cle: 'parc',
    titre: 'La façade côté parc',
    legende:
      "Côté prairies, exposée aux embruns de pollution et à l'humidité qui monte des pâtures.",
    altAvant: `Façade côté parc d'un centre équestre à ${ville} ternie par les dépôts avant lavage`,
    altApres: `Façade côté parc d'un centre équestre à ${ville} propre et uniforme après lavage`,
  },
];

// ── Points du hero (liste à coches, design accueil) ──
const points = [
  'Nettoyage de façade sur les trois murs du bâtiment principal',
  'Démoussage des algues, mousses et lichens',
  'Applicateur certifié DALEP',
];

// ── Fiche chantier (données factuelles → structure + confiance) ──
const fiche = [
  { label: 'Commune', valeur: `${ville} (28)`, icone: 'pin' },
  { label: 'Bâtiment', valeur: 'Haras et centre équestre', icone: 'cheval' },
  { label: 'Intervention', valeur: 'Nettoyage de façades', icone: 'drop' },
  { label: 'Support', valeur: 'Enduit de finition', icone: 'mur' },
];

// ── Déroulé du chantier : le savoir-faire, étape par étape ──
const etapes = [
  {
    t: 'Lecture du support',
    d: "Avant la moindre goutte d'eau, je regarde de quoi le mur est fait et ce qu'il a subi : nature de l'enduit, fissures, zones creuses, hauteur de remontée du vert. C'est ce diagnostic qui décide de la pression et du produit, pas l'inverse.",
  },
  {
    t: 'Protection des abords',
    d: "Boxes, paddocks, abreuvoirs et plantations sont repérés puis protégés, et les chevaux tenus à distance de la zone traitée. Sur un site vivant, l'organisation du chantier compte autant que le lavage lui-même.",
  },
  {
    t: 'Traitement et lavage',
    d: "Application d'un produit qui détruit mousses, lichens et algues jusque dans les pores de l'enduit, temps de pose respecté, puis rinçage à pression maîtrisée. La salissure part, l'enduit reste.",
  },
  {
    t: 'Reprise des détails et repli',
    d: "Angles, appuis de fenêtres, soubassements et sous-face de débord sont repris à la main. Le chantier est rendu nettoyé, sans résidus ni bidons abandonnés dans l'herbe.",
  },
];

// ── Balisage des 6 photos du chantier (feature Google « Image metadata ») ──
// Chaque photo est créditée à l'entreprise, située à Guainville et rattachée
// aux mentions légales pour les conditions de réutilisation.
const lieuChantier = {
  '@type': 'Place',
  name: `Haras à ${ville}`,
  address: {
    '@type': 'PostalAddress',
    addressLocality: ville,
    postalCode: cp,
    addressRegion: SITE.region,
    addressCountry: 'FR',
  },
};

const pageLicence = `${SITE.url}/mentions-legales/`;

const schemaPhoto = (cle: string, etat: string, nom: string, legende: string, principale = false) => ({
  '@context': 'https://schema.org',
  '@type': 'ImageObject',
  '@id': `${pageUrl}#photo-${cle}-${etat}`,
  contentUrl: `${SITE.url}${img(`${cle}-${etat}`)}`,
  url: pageUrl,
  name: nom,
  caption: legende,
  encodingFormat: 'image/webp',
  contentLocation: lieuChantier,
  creator: { '@id': businessId },
  copyrightHolder: { '@id': businessId },
  creditText: SITE.nom,
  copyrightNotice: `© ${SITE.nomLegal}`,
  license: pageLicence,
  acquireLicensePage: pageLicence,
  ...(principale ? { representativeOfPage: true } : {}),
});

const schemasPhotos = comparaisons.flatMap((c) => [
  schemaPhoto(c.cle, 'avant', `${c.titre} avant nettoyage — ${ville} (${cp})`, c.altAvant),
  schemaPhoto(c.cle, 'apres', `${c.titre} après nettoyage — ${ville} (${cp})`, c.altApres, c.cle === 'entree'),
]);

const schemaRealisation = {
  '@context': 'https://schema.org',
  '@type': 'Service',
  '@id': `${pageUrl}#realisation`,
  serviceType: 'Nettoyage de façade',
  name: `Nettoyage des façades d'un haras à ${ville}`,
  description:
    `Chantier de nettoyage de façades réalisé par Renaud Rénov sur un haras de ${ville} (${cp}), en Eure-et-Loir : diagnostic du support, traitement des mousses, lichens et algues, lavage à pression maîtrisée et reprise des détails.`,
  provider: { '@id': businessId },
  areaServed: { '@type': 'City', name: ville, postalCode: cp },
  image: schemasPhotos.map((ph) => ({ '@id': ph['@id'] })),
};
---
<BaseLayout title={title} description={description} extraSchemas={[schemaRealisation, ...schemasPhotos]}>
  <!-- ── Fil d'Ariane (BreadcrumbList émis par le composant, une seule fois) ── -->
  <section class="section section--compact">
    <div class="conteneur">
      <FilAriane
        items={[
          { label: 'Accueil', href: '/' },
          { label: 'Réalisations', href: '/realisations/' },
          { label: `Nettoyage de façade à ${ville}` },
        ]}
      />
    </div>
  </section>

  <!-- ── Hero : design de la page d'accueil (titre / photo / coches) ── -->
  <section class="hx">
    <div class="conteneur hx__grid">
      <div class="hx__head">
        <h1 class="hx__title">Nettoyage de façade réalisé à {ville} dans un haras</h1>
        <p class="hx__marque">RENAUD RÉNOV</p>
      </div>

      <div class="hx__right">
        <img
          class="hx__photo"
          src={img('entree-apres')}
          alt={`Façade d'un haras à ${ville} nettoyée par Renaud Rénov, enduit ravivé sous un ciel bleu`}
          width="1600" height="1200"
          fetchpriority="high"
        />
        <span class="hx__etiquette">Après</span>
        <div class="hx__note">
          <div class="hx__note-top">
            <LogoGoogle taille={26} />
            <span class="hx__note-stars" aria-label={`${reviews.note} étoiles sur 5`}>
              <svg viewBox="0 0 24 24" aria-hidden="true"><path d="M12 2l2.9 6.26 6.9.6-5.2 4.53 1.55 6.75L12 17.77 5.85 20.7 7.4 13.95 2.2 9.42l6.9-.6L12 2z"/></svg>
              <svg viewBox="0 0 24 24" aria-hidden="true"><path d="M12 2l2.9 6.26 6.9.6-5.2 4.53 1.55 6.75L12 17.77 5.85 20.7 7.4 13.95 2.2 9.42l6.9-.6L12 2z"/></svg>
              <svg viewBox="0 0 24 24" aria-hidden="true"><path d="M12 2l2.9 6.26 6.9.6-5.2 4.53 1.55 6.75L12 17.77 5.85 20.7 7.4 13.95 2.2 9.42l6.9-.6L12 2z"/></svg>
              <svg viewBox="0 0 24 24" aria-hidden="true"><path d="M12 2l2.9 6.26 6.9.6-5.2 4.53 1.55 6.75L12 17.77 5.85 20.7 7.4 13.95 2.2 9.42l6.9-.6L12 2z"/></svg>
              <svg viewBox="0 0 24 24" aria-hidden="true"><path d="M12 2l2.9 6.26 6.9.6-5.2 4.53 1.55 6.75L12 17.77 5.85 20.7 7.4 13.95 2.2 9.42l6.9-.6L12 2z"/></svg>
            </span>
          </div>
          <p class="hx__note-txt">Noté <strong>{reviews.note.toLocaleString('fr-FR')}/5</strong> sur Google · {reviews.nombre} avis</p>
        </div>
      </div>

      <div class="hx__body">
        <p class="hx__chapo">
          Trois façades gagnées par le vert, les lichens et les coulures de ruissellement, revenues à
          leur teinte d'origine sans une goutte de peinture. Retour sur une intervention de nettoyage
          démoussage de façade menée dans un haras de {ville}, près de mon secteur de
          <a href="/villes/dreux/">couvreur à Dreux</a>.
        </p>

        <ul class="hx__points">
          {points.map((pt) => (
            <li>
              <span class="hx__check"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect x="3" y="3" width="18" height="18" rx="4"/><path d="M8 12.5l2.5 2.5 5-6"/></svg></span>
              {pt}
            </li>
          ))}
        </ul>

        <div class="hx__badges">
          <img src="/images/labels/applicateur-dalep.webp" alt="Applicateur de solutions DALEP certifié" width="240" height="240" loading="lazy" />
        </div>

        <div class="hx__actions">
          <a href="/devis-gratuit/" class="btn btn--rouge">Devis gratuit<FlecheRonde /></a>
          <a href={SITE.telHref} class="btn btn--bleu">
            <svg class="icone" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.9" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M6.5 3.5h-2A1.5 1.5 0 0 0 3 5.2C3.4 13 11 20.6 18.8 21a1.5 1.5 0 0 0 1.7-1.5v-2a1.5 1.5 0 0 0-1.3-1.5l-2.5-.35a1.5 1.5 0 0 0-1.4.6l-.7.95a11.5 11.5 0 0 1-4.9-4.9l.95-.7a1.5 1.5 0 0 0 .6-1.4l-.35-2.5a1.5 1.5 0 0 0-1.5-1.3Z"/><path d="M14.5 3.6a5.5 5.5 0 0 1 4.9 4.9"/><path d="M14 7a2.4 2.4 0 0 1 2 2"/></svg>
            {SITE.tel}
          </a>
        </div>
        <p class="hx__cta-sub">Réponse rapide, sans engagement</p>
      </div>
    </div>
  </section>

  <!-- ── Fiche chantier : cartes en chevauchement du hero ─────── -->
  <section class="section reno-fiche-wrap reno-fiche-wrap--clair">
    <div class="conteneur">
      <ul class="reno-fiche">
        {fiche.map((f) => (
          <li>
            <span class="reno-fiche__icone" aria-hidden="true">
              {f.icone === 'pin' && <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M20 10c0 6-8 12-8 12s-8-6-8-12a8 8 0 0 1 16 0Z"/><circle cx="12" cy="10" r="3"/></svg>}
              {f.icone === 'cheval' && <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M4 21c0-4.2 2.2-7.4 5.6-9L8 8.4 5.6 9.6 4.4 7.4 9 5l1.6 1.2A6 6 0 0 1 20 11v10"/><path d="M9 5.2 8.4 3l2.6.8"/></svg>}
              {f.icone === 'drop' && <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 2s6 6.5 6 11a6 6 0 0 1-12 0c0-4.5 6-11 6-11Z"/></svg>}
              {f.icone === 'mur' && <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="4" width="18" height="16" rx="1.5"/><path d="M3 9.3h18M3 14.7h18M9 4v5.3M15 9.3v5.4M9 14.7V20"/></svg>}
            </span>
            <span class="reno-fiche__txt">
              <span class="reno-fiche__label">{f.label}</span>
              <strong>{f.valeur}</strong>
            </span>
          </li>
        ))}
      </ul>
    </div>
  </section>

  <!-- ── Ce que les murs avaient accumulé ─────────────────────── -->
  <section class="section">
    <div class="conteneur">
      <div class="section__entete">
        <h2>Pourquoi ces façades avaient besoin d'un démoussage</h2>
      </div>
      <p class="reno-intro">
        Au nord de l'{SITE.departement}, entre les pâtures et la vallée de l'Eure, les façades
        prennent l'humidité de plein fouet. Sur ce haras, le diagnostic était le même sur les trois
        murs&nbsp;: un voile vert d'algues sur les parties basses, des plaques de lichen sur les
        appuis, et de longues traînées rouges parties du haut du pignon.
      </p>
      <p class="reno-intro">
        Ces dépôts ne sont pas qu'une question de regard. Ils retiennent l'eau contre l'enduit,
        l'empêchent de sécher entre deux pluies et finissent par le faire cloquer puis se détacher.
        Nettoyer un mur à temps, c'est éviter d'avoir à le refaire.
      </p>
    </div>
  </section>

  <!-- ── Avant / après : la preuve, en curseurs à faire glisser ── -->
  <section class="section section--gris">
    <div class="conteneur">
      <div class="section__entete">
        <h2>Démoussage de façade avant / après&nbsp;: les trois murs du haras</h2>
      </div>
      <p class="reno-intro">
        Chaque photo a été prise deux fois, au même endroit&nbsp;: à mon arrivée, puis le chantier
        terminé. Passez la souris sur une image pour révéler le résultat, ou glissez le doigt sur
        mobile.
      </p>

      <div class="reno-compare">
        {comparaisons.map((c) => (
          <figure class="reno-compare__item">
            <AvantApresSlider
              avantSrc={img(`${c.cle}-avant`)}
              apresSrc={img(`${c.cle}-apres`)}
              avantAlt={c.altAvant}
              apresAlt={c.altApres}
              width={1600}
              height={1200}
            />
            <figcaption>
              <strong>{c.titre}</strong>
              {c.legende}
            </figcaption>
          </figure>
        ))}
      </div>
    </div>
  </section>

  <!-- ── Déroulé du chantier ──────────────────────────────────── -->
  <section class="section">
    <div class="conteneur">
      <div class="section__entete">
        <h2>Du diagnostic à l'application des produits, étape par étape</h2>
      </div>
      <p class="reno-intro">
        Laver une façade sans l'abîmer demande de la retenue&nbsp;: trop de pression et l'enduit part
        avec la mousse. Voici les quatre temps de cette
        <a href="/nettoyage-facade-28/">prestation de nettoyage de façade</a>, telle que je
        l'ai menée à {ville}.
      </p>
      <ol class="reno-etapes">
        {etapes.map((e, i) => (
          <li class="reno-etape">
            <span class="reno-etape__num">{i + 1}</span>
            <div>
              <h3>{e.t}</h3>
              <p>{e.d}</p>
            </div>
          </li>
        ))}
      </ol>
    </div>
  </section>

  <!-- ── Le détail propre à ce chantier : travailler avec des chevaux ── -->
  <section class="section section--gris">
    <div class="conteneur">
      <div class="reno-encart">
        <div class="reno-encart__txt">
          <h2>Traiter les façades d'un haras sans déranger les chevaux</h2>
          <p>
            Un haras n'est pas une maison vide. Les chevaux de sport sont sensibles au bruit, aux
            odeurs et aux mouvements inhabituels&nbsp;; les produits, eux, ne doivent atteindre ni les
            abreuvoirs ni les pâtures. Les zones traitées ont donc été isolées avant chaque phase, le
            travail organisé façade par façade pour libérer les accès au fur et à mesure, et les
            abords rincés en fin de journée.
          </p>
          <p>
            C'est le genre de contrainte qui ne se voit pas sur les photos, mais qui décide si un
            chantier se passe bien ou non — sur une exploitation agricole, un centre équestre comme
            sur une maison habitée.
          </p>
        </div>
        <ul class="reno-encart__liste">
          <li>Façades nettoyées dans leur intégralité</li>
          <li>Mousses, lichens et salissures éliminés</li>
          <li>Méthode adaptée au support, sans agresser l'enduit</li>
          <li>Chantier propre, abords protégés et rincés</li>
        </ul>
      </div>
    </div>
  </section>

  <!-- ── Passerelle vers la prestation + l'autre réalisation ──── -->
  <section class="section">
    <div class="conteneur">
      <div class="section__entete">
        <h2>Le même nettoyage de façade chez vous, en {SITE.departement}</h2>
      </div>
      <p class="reno-intro">
        Ce que ces murs ont retrouvé en une intervention, une façade de maison, un pignon de pavillon
        ou un bâtiment d'exploitation peut le retrouver aussi. Le principe ne change pas&nbsp;: on
        lit le support, on traite, on lave sans forcer.
      </p>
      <div class="reno-liens">
        <a class="reno-lien" href="/nettoyage-facade-28/">
          <strong>Nettoyage de façade en {SITE.departement}</strong>
          <span class="reno-lien__d">Supports traités, méthode, tarifs et devis gratuit.</span>
        </a>
        <a class="reno-lien" href="/realisations/demoussage-toiture-maintenon/">
          <strong>Démoussage d'une toiture à Maintenon</strong>
          <span class="reno-lien__d">Même logique, appliquée cette fois à une couverture en tuiles.</span>
        </a>
      </div>
      <p class="reno-signature">
        J'interviens auprès des particuliers, des exploitations agricoles et des centres équestres de
        l'{SITE.departement}&nbsp;({SITE.departementNum}), des Yvelines&nbsp;(78) et de
        l'Eure&nbsp;(27).
      </p>
    </div>
  </section>

  <!-- ── CTA devis ────────────────────────────────────────────── -->
  <CtaDevis
    titre={`Une façade à nettoyer du côté de ${ville} ?`}
    image="/images/photos/hero-renaud-renov-640.webp"
    imageAlt={`Renaud Rénov, artisan intervenant sur les façades à ${ville} et en ${SITE.departement}`}
    imageWidth={640}
    imageHeight={480}
  />
</BaseLayout>

<style>
  /* ══ Hero — design de la page d'accueil (repris de index.astro) ══ */
  .hx { padding-block: clamp(1.6rem, 4vw, 3rem); }
  .hx__grid {
    display: grid;
    gap: 1.6rem;
    grid-template-columns: 1fr;
    grid-template-areas: "head" "photo" "body";
  }
  @media (min-width: 980px) {
    .hx__grid {
      grid-template-columns: 1.05fr 1fr;
      grid-template-areas: "head photo" "body photo";
      column-gap: 3.5rem;
      row-gap: 1.1rem;
      align-items: center;
    }
  }
  .hx__head { grid-area: head; }
  .hx__right { grid-area: photo; position: relative; }
  .hx__right::after {
    content: "";
    position: absolute;
    left: 0; right: 0; bottom: 0;
    height: 42%;
    border-radius: 0 0 24px 24px;
    background: linear-gradient(to top, rgba(15, 34, 45, 0.6), rgba(15, 34, 45, 0));
    pointer-events: none;
    z-index: 1;
  }
  .hx__body { grid-area: body; }
  .hx__title { margin: 0; color: var(--bleu); font-size: clamp(1.6rem, 3.2vw, 2.35rem); line-height: 1.2; letter-spacing: -0.01em; }
  .hx__marque { color: var(--rouge); font-weight: 800; font-size: clamp(1.9rem, 4.4vw, 2.9rem); line-height: 1.1; margin: 0.6rem 0 0; }
  .hx__photo { width: 100%; aspect-ratio: 4 / 3; object-fit: cover; border-radius: 24px; box-shadow: var(--ombre-forte); display: block; }
  @media (min-width: 980px) { .hx__photo { aspect-ratio: 4 / 5; } }

  /* Étiquette « Après » sur la photo du hero */
  .hx__etiquette {
    position: absolute;
    top: 1rem; left: 1rem;
    z-index: 2;
    padding: 0.35rem 0.85rem;
    border-radius: 999px;
    background: var(--rouge);
    color: var(--blanc);
    font-size: var(--fs-xs);
    font-weight: 700;
  }

  .hx__chapo { color: var(--gris-700); margin: 0 0 1.2rem; max-width: var(--mesure-etroite); }
  .hx__chapo a { color: var(--rouge-fonce); font-weight: 600; }

  .hx__points { list-style: none; padding: 0; margin: 0.4rem 0 1.2rem; display: grid; gap: 0.9rem; }
  .hx__points li {
    display: flex; align-items: center; gap: 0.8rem;
    font-size: var(--fs-md); font-weight: 600; color: var(--bleu);
  }
  .hx__check { color: var(--rouge); flex-shrink: 0; line-height: 0; }
  .hx__check svg { width: 30px; height: 30px; }

  .hx__note {
    position: absolute;
    left: 50%;
    bottom: 1rem;
    transform: translateX(-50%);
    width: max-content;
    max-width: calc(100% - 1.6rem);
    display: flex; flex-direction: column; align-items: center; gap: 0.3rem;
    background: var(--blanc);
    border-radius: 16px;
    box-shadow: 0 14px 34px rgba(15, 34, 45, 0.42);
    padding: 0.8rem 1.5rem;
    z-index: 2;
  }
  .hx__note-top { display: flex; align-items: center; gap: 0.6rem; }
  .hx__note-stars { display: inline-flex; gap: 0.1em; }
  .hx__note-stars svg { width: 1.35rem; height: 1.35rem; fill: var(--etoile); }
  .hx__note-txt { margin: 0; color: var(--gris-700); font-size: var(--fs-base); }
  .hx__note-txt strong { color: var(--bleu); }

  .hx__badges { display: flex; align-items: center; gap: 1.3rem; flex-wrap: wrap; margin: 0 0 1.6rem; }
  .hx__badges img { height: 64px; width: auto; }

  .hx__actions { display: flex; flex-wrap: wrap; gap: 0.8rem; }
  .hx__cta-sub { color: var(--rouge-fonce); font-size: var(--fs-sm); margin: 0.5rem 0 0; }

  @media (max-width: 979px) {
    .hx__title, .hx__marque { text-align: center; }
    .hx__chapo { margin-inline: auto; } /* chapô long : reste en drapeau à gauche */
    .hx__badges, .hx__actions { justify-content: center; }
    .hx__cta-sub { text-align: center; }
    .hx__badges img { height: 56px; }
  }

  /* ══ Fiche chantier (chevauche le hero) ══ */
  .reno-fiche-wrap { padding-top: 0; padding-bottom: 0; }
  .reno-fiche-wrap--clair .reno-fiche li { box-shadow: 0 8px 22px rgba(36, 75, 100, 0.09); }
  .reno-fiche {
    list-style: none;
    padding: 0;
    margin: 0;
    position: relative;
    z-index: 2;
    display: grid;
    gap: 1rem;
    grid-template-columns: repeat(auto-fit, minmax(210px, 1fr));
  }
  .reno-fiche li {
    display: flex;
    align-items: center;
    gap: 0.9rem;
    background: #fff;
    border: 1px solid rgba(36, 75, 100, 0.08);
    border-radius: 16px;
    padding: 1.1rem 1.25rem;
    box-shadow: 0 12px 30px rgba(36, 75, 100, 0.14);
  }
  .reno-fiche__icone {
    flex: 0 0 auto;
    width: 44px; height: 44px;
    display: inline-flex; align-items: center; justify-content: center;
    border-radius: 12px;
    background: var(--rouge-pale);
    color: var(--rouge);
  }
  .reno-fiche__icone svg { width: 1.4rem; height: 1.4rem; }
  .reno-fiche__txt { display: flex; flex-direction: column; line-height: 1.3; }
  .reno-fiche__label { font-size: var(--fs-xs); text-transform: uppercase; letter-spacing: 0.05em; color: var(--gris-500); }
  .reno-fiche__txt strong { color: var(--bleu); font-size: var(--fs-base); }

  /* ══ Intro de section ══ */
  .reno-intro {
    max-width: var(--mesure);
    margin: 0 auto 1.4rem;
    /* drapeau à gauche : au-delà de 2 lignes, un texte centré oblige
       l'œil à rechercher le début de chaque ligne */
    text-align: left;
    text-wrap: pretty;
    color: var(--gris-700);
  }
  .reno-intro:last-of-type { margin-bottom: 0; }

  /* ══ Comparateurs avant / après ══ */
  .reno-compare {
    display: grid;
    gap: clamp(1.4rem, 3vw, 2.2rem);
    margin-top: 2rem;
  }
  @media (min-width: 900px) {
    .reno-compare { grid-template-columns: repeat(2, 1fr); }
    /* Le premier comparateur occupe toute la largeur : c'est la photo maîtresse. */
    .reno-compare__item:first-child { grid-column: 1 / -1; }
  }
  .reno-compare__item { margin: 0; }
  .reno-compare figcaption {
    margin-top: 0.9rem;
    color: var(--gris-700);
    font-size: var(--fs-sm);
    line-height: 1.55;
  }
  .reno-compare figcaption strong {
    display: block;
    color: var(--bleu);
    font-size: var(--fs-base);
    margin-bottom: 0.2rem;
  }

  /* ══ Déroulé en étapes ══ */
  .reno-etapes {
    list-style: none;
    padding: 0;
    margin: 2.2rem 0 0;
    display: grid;
    gap: 1.2rem;
    grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
  }
  .reno-etape {
    background: #fff;
    border-radius: 18px;
    padding: 1.9rem 1.7rem;
    box-shadow: 0 10px 28px rgba(36, 75, 100, 0.10);
  }
  .reno-etape__num {
    display: inline-flex;
    align-items: center; justify-content: center;
    width: 42px; height: 42px;
    border-radius: 50%;
    background: var(--bleu);
    color: #fff;
    font-weight: 800;
    font-size: var(--fs-md);
    margin-bottom: 1rem;
  }
  .reno-etape h3 { margin: 0 0 0.5rem; color: var(--bleu); }
  .reno-etape p { margin: 0; color: var(--gris-700); }

  /* ══ Encart « particularité du chantier » ══ */
  .reno-encart {
    display: grid;
    gap: clamp(1.5rem, 4vw, 2.6rem);
    align-items: center;
    background: #fff;
    border-radius: 20px;
    padding: clamp(1.7rem, 4vw, 2.8rem);
    box-shadow: 0 14px 34px rgba(36, 75, 100, 0.12);
  }
  @media (min-width: 900px) {
    .reno-encart { grid-template-columns: 1.35fr 1fr; }
  }
  .reno-encart h2 { margin: 0 0 0.9rem; }
  .reno-encart p { color: var(--gris-700); margin: 0 0 0.9rem; }
  .reno-encart p:last-child { margin-bottom: 0; }
  .reno-encart__liste {
    list-style: none;
    margin: 0;
    padding: clamp(1.2rem, 3vw, 1.6rem);
    background: var(--bleu-pale);
    border-radius: 16px;
    display: grid;
    gap: 0.85rem;
  }
  .reno-encart__liste li {
    display: flex;
    gap: 0.6rem;
    align-items: flex-start;
    color: var(--bleu);
    font-weight: 600;
    line-height: 1.4;
  }
  .reno-encart__liste li::before {
    content: '✓';
    color: var(--rouge-fonce);
    font-weight: 800;
    flex: 0 0 auto;
  }

  /* ══ Passerelles de maillage ══ */
  .reno-liens {
    display: grid;
    gap: 1.1rem;
    grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
    margin-top: 2rem;
  }
  .reno-lien {
    display: block;
    background: #fff;
    border: 1px solid var(--gris-200);
    border-radius: 18px;
    padding: 1.5rem 1.6rem;
    text-decoration: none;
    box-shadow: 0 10px 26px rgba(36, 75, 100, 0.08);
    transition: transform var(--transition), box-shadow var(--transition), border-color var(--transition);
  }
  .reno-lien:hover {
    transform: translateY(-4px);
    border-color: transparent;
    box-shadow: 0 16px 36px rgba(36, 75, 100, 0.16);
  }
  .reno-lien strong { display: block; color: var(--bleu); font-size: var(--fs-md); margin-bottom: 0.4rem; }
  .reno-lien__d { display: block; color: var(--gris-700); font-size: var(--fs-sm); line-height: 1.5; }

  .reno-signature {
    max-width: var(--mesure-etroite);
    margin: 2.4rem auto 0;
    text-align: left;
    text-wrap: pretty;
    font-size: var(--fs-md);
    font-weight: 600;
    color: var(--bleu);
  }

  /* ══ Mobile : mêmes règles de centrage que le reste du site ══ */
  @media (max-width: 800px) {
    .reno-encart__liste { order: -1; }
  }

  /* ── Mesure de lecture ────────────────────────────────────────
     En dessous du seuil où ce bloc passe en deux colonnes, il occupe
     toute la largeur du conteneur et la ligne dépassait 90 caractères
     (le confort de lecture s'arrête vers 75). On borne la colonne. */
  .reno-encart__txt p { max-width: var(--mesure); }
</style>
