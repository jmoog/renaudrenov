---
// ── Redirection 301 permanente (13/08/2026) ─────────────────────────────
// La page a déménagé : /prestations/traitement-hydrofuge/
// → /traitement-hydrofuge-28/ (slug à plat, aligné sur le pilier
// /nettoyage-toiture-28/).
// prerender = false : la route est servie par l'adapter Node au runtime,
// ce qui produit un VRAI statut HTTP 301 (et non une meta-refresh statique).
export const prerender = false;

return Astro.redirect('/traitement-hydrofuge-28/', 301);
---
