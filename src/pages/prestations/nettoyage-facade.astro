---
// ── Redirection 301 permanente (13/08/2026) ─────────────────────────────
// La page a déménagé : /prestations/nettoyage-facade/ → /nettoyage-facade-28/
// (slug à plat, brief pilier secondaire du cocon façade).
// prerender = false : la route est servie par l'adapter Node au runtime,
// ce qui produit un VRAI statut HTTP 301 (et non une meta-refresh statique).
export const prerender = false;

return Astro.redirect('/nettoyage-facade-28/', 301);
---
