---
// ── Redirection 301 permanente (14/08/2026) ─────────────────────────────
// La page a déménagé : /prestations/pose-gouttiere/ → /pose-gouttiere-28/
// (slug à plat, dernier pilier du cocon couverture).
// prerender = false : la route est servie par l'adapter Node au runtime,
// ce qui produit un VRAI statut HTTP 301 (et non une meta-refresh statique).
export const prerender = false;

return Astro.redirect('/pose-gouttiere-28/', 301);
---
